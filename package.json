{
  "name": "spacelord-prototype",
  "private": true,
  "version": "1.0.0",
  "description": "SpaceLord — a hackathon prototype of private & secure smart space sharing (from the ACSAC'22 paper).",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run start",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
